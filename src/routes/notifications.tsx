import { createFileRoute } from "@tanstack/react-router";
import { ArrowLeft } from "lucide-react";
import { useNotificationActions, useNotifications } from "@/hooks/useWalletData";
export const Route=createFileRoute("/notifications")({component:Notifications});
function Notifications(){const {notifications,isLoading}=useNotifications();const {markRead,markAllRead}=useNotificationActions();return <main className="mx-auto max-w-lg px-4 pt-5 pb-24"><a href="/" className="inline-flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft size={17}/> Dashboard</a><div className="mt-5 flex items-center justify-between"><h1 className="text-2xl font-semibold">Notifications</h1><button onClick={()=>markAllRead.mutate()} className="text-xs text-primary">Mark all read</button></div><div className="mt-5 divide-y divide-border rounded-2xl border border-border bg-card">{isLoading?<p className="p-5">Loading…</p>:notifications.length===0?<p className="p-5 text-sm text-muted-foreground">No notifications.</p>:notifications.map(n=><button key={n.id} onClick={()=>!n.read&&markRead.mutate(n.id)} className={`block w-full p-4 text-left hover:bg-secondary ${!n.read?'bg-primary/5':''}`}><p className="font-medium">{n.title}</p><p className="mt-1 text-sm text-muted-foreground">{n.body}</p><p className="mt-2 text-xs text-muted-foreground">{new Date(n.created_at).toLocaleString()}</p></button>)}</div></main>}
