import type { AssetPosition } from "@/types/wallet";
import { formatAmount, formatFiat } from "@/lib/format";
import { CryptoLogo } from "./CryptoLogo";
import { PriceChange } from "./PriceChange";
interface AssetRowProps { position: AssetPosition; showHolding?: boolean; onSelect?: (position: AssetPosition) => void; }
const NETWORKS:Record<string,string>={BTC:"Bitcoin",ETH:"Ethereum (ERC-20)",USDT:"Ethereum (ERC-20)",BNB:"BNB Smart Chain (BEP-20)",SOL:"Solana",XRP:"XRP Ledger",USDC:"Ethereum (ERC-20)",ADA:"Cardano",DOGE:"Dogecoin",TRX:"TRON (TRC-20)",AVAX:"Avalanche C-Chain",LINK:"Ethereum (ERC-20)",POL:"Polygon",LTC:"Litecoin",DOT:"Polkadot"};
export function AssetRow({position,showHolding=true,onSelect}:AssetRowProps){const{market,balance,fiatValue}=position;const network=NETWORKS[market.symbol]??"Supported network";return <li><button type="button" onClick={()=>onSelect?.(position)} aria-label={`${market.name} details`} className="flex w-full items-center gap-3 rounded-[10px] px-3 py-3 text-left transition-colors hover:bg-secondary focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring"><CryptoLogo src={market.image} symbol={market.symbol}/><span className="min-w-0 flex-1"><span className="block truncate text-sm font-medium">{market.name}</span><span className="block truncate text-xs font-semibold text-foreground">{market.symbol}/USDT</span><span className="block truncate text-[11px] text-muted-foreground">{network}</span></span><span className="text-right">{showHolding?<><span className="block text-sm font-medium tabular-nums">{formatAmount(balance,market.symbol)}</span><span className="block text-xs tabular-nums text-muted-foreground">{formatFiat(fiatValue)}</span></>:<span className="block text-sm font-medium tabular-nums">{formatFiat(market.price)}</span>}<PriceChange value={market.changePercent24h} className="block text-xs"/></span></button></li>}
